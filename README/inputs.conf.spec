[progress_whatsup_gold://<name>]
device_reports = Pick the reports you'd like to fetch.
time_range = Typically, keep "TIme Range" and Interval as same amount to avoid querying same data multiple times.
range_n = Default is "1". Multiplier for the last"N" kind timerange.
debug = Turn it on to produce more events/logs to identify any issues or reset the checkpoint.